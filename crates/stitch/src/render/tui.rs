//! Full-screen TUI mode (requires `tui` feature).
//!
//! Provides a ratatui-based interface with split panes for
//! conversation, file viewer, and activity log.

/// Run the TUI event loop.
pub async fn run() -> anyhow::Result<()> {
    tracing::info!("tui mode — not implemented yet");
    Ok(())
}
